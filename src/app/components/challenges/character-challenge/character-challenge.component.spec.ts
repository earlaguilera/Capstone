/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CharacterChallengeComponent } from './character-challenge.component';

describe('Component: CharacterChallenge', () => {
  it('should create an instance', () => {
    let component = new CharacterChallengeComponent();
    expect(component).toBeTruthy();
  });
});
